---
name: hermes-memory-cleanup
description: "Hermes 记忆批量清理：审计→归类→建文档→删冗余→瘦身，从膨胀到清爽的六步工作流。"
version: 1.0.0
---

# Hermes 记忆批量清理工作流

> 当 Memory 膨胀时（30+ 条目，占用 >50%），执行六步清理，最终只保留索引 + 关键速查。

## 触发条件

- 用户说「清理记忆」「整理记忆」「记忆太多了」
- 记忆占用超过 50% 且条目分散、重复
- 发现大量过时或不再需要的信息

## 六步工作流

### 第一步：审计现有记忆

**先让用户确认要删什么、留什么。不要自己猜。**

```
clarify("要清理哪些内容？", choices=["过时信息", "已完成的项目", "重复条目", "全部审核一遍"])
```

同时自己扫描，给每一条打标：

| 标记 | 含义 | 行动 |
|------|------|------|
| 🔴 | 用户明确要删的 | 直接删除 |
| 🟡 | 重复的（两条说同一件事） | 保留信息更全的那条，删另一条 |
| 🟢 | 需要归档到文档的 | 移入文档后删除 |
| ⭐ | 必须留在内存的关键速查 | 保留，精简 |

### 第二步：归类剩余条目

把 🟢 条目按主题分组。常见分类：

| 分类 | 示例 |
|------|------|
| 系统运维 | 服务器配置、SSH、进程管理、安全策略 |
| 项目信息 | 项目架构、技术栈、里程碑、品牌素材 |
| 用户偏好 | 审美偏好、工作习惯、沟通风格 |
| 环境配置 | API 端点、模型配置、工具路径 |

### 第三步：创建整合文档

每个分类创建一个 `.md` 文档，放在 `~/` 下（敏感内容放 `private/`）。

**文档模板**：

```markdown
# 标题

> 一句话描述

---

## 子主题 A

### 具体条目
- 要点
- 注意

## 子主题 B
...
```

**命名规范**：描述性文件名，如 `~/system-ops-notes.md`、`~/project-notes.md`

**关键**：文档自包含、结构化——将来不看原始记忆也能用。

### 第四步：更新索引

在索引文件中追加新文档：

```markdown
| N | 文档名 | 路径 | 内容概述 |
```

同时更新 Memory 中的索引描述（文档总数、分类）。

### 第五步：批量删除内存条目

用 `memory(action='remove', old_text='...')` 逐条删除已归档条目。

**old_text 选法技巧**：

- ✅ 取条目开头 **10-20 个字符**，足够唯一即可
- ⚠️ 避免 Windows 路径中的反斜杠 `\`（可能匹配失败）
- ✅ 用关键词而非路径（如用 `QtScrcpy` 而非 `D:\king`）
- ✅ 匹配失败时换更短/更独特的子串重试
- ✅ 每次删除 4 条一组，提高效率

**执行顺序**：先清理 Memory（target='memory'），再清理 User Profile（target='user'）。

### 第六步：验证结果

检查内存占用：

```
期望结果：
✅ 占用率从 >50% 降到 <10%
✅ 条目数降到 2-5 条
✅ 只剩：索引指针（1条）+ 关键速查（1-2条）
```

**最终保留原则**：

| 保留 | 数量 | 示例 |
|------|------|------|
| 索引指针 | 1 条 | `所有文档索引见 ~/.../memory-index.md` |
| 高频速查 | 1-2 条 | 常用参数摘要（<200 字） |
| **总计** | **2-3 条** | |

其他全部走文档，`read_file` 按需读取。

---

## 实战参考

### 案例：30+ 条目瘦身

**清理前**：30+ 条目，60% 占用
**清理后**：2 条目，4% 占用

**过程**：
1. 用户指定删除过时偏好 ×5
2. 归类剩余：系统运维类 → `system-ops-notes.md`，项目偏好类 → `project-and-preferences.md`
3. 批量删除已归档条目 ×13
4. 索引文件 15→17 份文档

---

## 注意事项

- 🚫 不要删 User Profile 中的行为偏好和底线规则——这些直接影响 agent 行为
- ✅ 删除前确认独立文档已完整覆盖对应内容
- ✅ 索引文件和 Memory 同步更新（否则指针失效）
- ✅ 删除不可逆，批量操作前确认用户意图
- ✅ 清理完成后告知用户前后对比（占用率、条目数）

## 与 hermes-memory-management 的关系

`hermes-memory-management` 定义日常「文档优先」的理念和新增信息的处理规则。本技能专注「膨胀→瘦身」的批量清理场景。

日常用前者，积累膨胀后用本技能大扫除。
